# Fecho 项目总结

> Agent 优先的工作日志系统 · [AI-2541](https://mobius.feedmob.com/issue/AI-2541)
> 3914 行代码 / 78 个单测 / 16 个提交 · 本机跑过 6 天真实数据、267 条进展

---

## 一、要解决什么

写日报这件事，难的不是写，是**回忆**。一天干完，人已经想不起早上那两个小时到底在解决什么。

于是有了这个设想：**agent 边干活边记，人只做最后一步**。

```
agent 边干边记 ──▶ 系统归到任务上 ──▶ 日终整理 ──▶ 日报 + 口播稿 ──▶ 人照读录语音
 log_progress      语义判断归属        cron / LLM     按任务分组      （唯一的人工环节）
```

系统的**用户是 agent，人是读者**。这个定位决定了几乎所有设计：提交入口只有 MCP 和 API，
没有网页表单；记的是「做成了什么」而不是对话原文；工具描述写给 agent 看，得说清楚什么时候调、
记什么粒度。

---

## 二、核心数据模型：任务，不是记录

存的主干是**任务**，不是一条条平铺的日志。

```
任务
 ├─ 名字
 ├─ 来源：Mobius 上的 issue（AI-2541） 或者 自由任务
 ├─ 状态
 └─ 进展 × N
      ├─ 谁记的（codex / claude-code / hermes）
      ├─ 什么时候、哪个对话
      └─ 做成了什么
```

一个任务横跨很多天、很多对话、很多 agent；一个对话里也可能推进好几个任务。
**所以任务是主干，会话只是审计线索。**

这个模型是中途改的。最初是「一条条记录」，改成「任务 + 多条进展」之后，日报才能按任务分组，
而不是流水账。

### 数据表

| 表 | 存什么 |
|---|---|
| `tasks` | 任务主干 |
| `updates` | 进展，挂在任务下 |
| `mobius_issues` | 本地缓存的在办 issue（配对候选） |
| `scan_marks` | 每个会话的扫描水位线 |
| `reports` / `report_history` | 日报、口播稿及其历史版本 |

---

## 三、两条录入路径

### 1. 主动记（`log_progress`）

agent 推进完一件事就调一次。即时返回，不等模型。

### 2. 自动扫（`fecho scan`）

cron 定时读 agent 的会话记录，用 LLM 抽出「做成了什么」。
**这是「你不说，它也记」的兜底**——agent 忘了记，扫描还能补回来。

配套两个机制：

- **水位线**：每个会话记到哪了，下次从那继续。某一组处理失败时，水位线**停在那组之前**，
  失败的那段和它之后的下次重来，不会丢。
- **工作范围白名单**：默认不扫任何目录，没登记的一律跳过——**只知道路径，不读内容**。

---

## 四、最曲折的部分：归属判断

「这条进展属于哪个 issue」是整个系统的命脉——**归属错了，下游全错**。这块推翻重做过一次。

### 第一版：字面相似度（已废弃）

拿进展的文字和 issue 标题比重合度，中文比 bigram 包含度、英文比 token、再加最长连续共现。

**两头都错：**

| 错误方向 | 真实案例 |
|---|---|
| 该配上的配不上 | 做 AI-2541 时说的是「配对引擎」「单测」「OAuth」，和标题「写一个提交工作日志的系统」**一个词都不重合** |
| 不该配上的配上了 | 闲鱼选品调研的内容被配进 AI-2541，唯一的共同点是两句话里都有 `agent` 这个词 |

第二类的打分细节值得记一笔：标题里只有 `agent` 和 `专用` 两个 latin token，命中一个就是 1/2
的召回率，乘 0.35 权重正好 0.35，越过 0.30 的门槛。

**我对此的第一反应是加一张「这些词不算数」的停用词表**——这是在背答案。换个人，他的 issue 叫
「优化投放 ROI」，撞车的会是「数据」「转化」「渠道」，表里没有，照样错。
**每个人的词汇表都不一样，这张表永远补不完。**

还有个更隐蔽的问题：一条配错会顺着「同会话惯性」把后面全带偏。实测「放开选品限制…」那条
分数 0.0 却也归了 AI-2541，就是继承了上一条的错误。

### 第二版：语义判断 + 事后交叉验证（现行）

判断「这句话说的是不是这件事」正是 LLM 擅长、字面比对天生做不到的。按路径分：

| 层 | 谁判 | 判什么 |
|---|---|---|
| **记录时** | 确定信号 | issue 号 / 目录绑定 / 同对话惯性（明确标为「猜的」） |
| **扫描时** | LLM（m3） | 看意思判归属，只能从候选列表里选 |
| **出稿前** | LLM（m3） | **全部重判一次**，含 agent 明确填的 |

**为什么连 `explicit` 也要重判**：那个 issue 号是调用方 agent 填的，**它同样是模型的判断，
一样会错**；而且记的时候手上只有当前这一条的上下文，出稿前能看到一整天。
不一致时以验证为准，但改动会如实报出来，不闷声改。

**为什么交互式那条路不调 LLM**：调用方 agent 本身就是 LLM，而且有完整上下文——
它知道自己在推进哪件事。让 MCP 再调一次模型去猜调用方已经知道的事，又慢又蠢。
所以工具描述里明确要求 agent 自己判、填 `issue` 参数，不确定就留空。

**防幻觉**：模型返回的 issue 号按**真实候选列表**校验，不只校验形状——
光看形状挡不住一个格式完全正确但根本不存在的号。

### 效果

拿 7 天真实数据重扫验证：

- **09-03 从 8 个碎任务收敛成 2 个真实 issue**
- **闲鱼选品调研自己找到了 AI-2224**「与小 Lu 商量，有关 Linux.do 积分…开小店」——
  两边字面零重合，关键词匹配永远配不上，看意思一眼就认出来
- 09-04 那天 lu3 服务器、awesome-gpt-image-2、freellmapi 各自归位

当前 259 条 active 进展的归属方式分布：

```
project-bound  115   目录绑定（人主动配的）
explicit       105   写明了 issue 号
task-continue   31   同对话惯性（标了「猜的」）
mobius-auto      5   ← 删除关键词配对前的历史残留
new-task         3   自由任务
```

落自由任务的比例 5%。**这个数字是判断「这套机制在某个人身上灵不灵」最直接的信号**，
dashboard 上专门有一块盯着它。

---

## 五、去重：判错了也不丢数据

扫描重跑（比如上次某组失败后补跑）会把同一段对话重新总结一遍，产生大量措辞不同、
信息量相同的复述。实测一天能在 85 条里堆出 158 对。

**但相似度是纯字符串判断，没有任何后续环节能纠错**——归属判错了还有 verify 那一步兜底，
去重判错了没有下文，因为内容压根没进过数据库：日报看不到、人也翻不到，
等于这条进展从没存在过。

所以现在判为近似重复的**照样写进库**，只标 `status='duplicate-ignored'`，
meta 里记下它被判成了谁的重复。下游查询本来就只看 `active`，日报不受影响。

配套 `fecho hidden` 能翻出来、`--restore` 能捞回来。**第一次跑就发现一个真实误判**：

> 「之前一次补丁把写入进展的整段代码删了，成功路径下什么都不写；测试没覆盖 `scan()` 主流程所以没抓到」

这是条独立的真实进展（还是个挺重要的教训），被当成复述清掉了。按老逻辑它已经永远消失。

两个例外保持原样：**逐字相同**的不重复写（那是 agent 重试，内容已原样在库）；
**近似去重只对扫描生效**（agent 主动记的相似内容可能是措辞碰巧像的不同进展，一条都不能少）。

---

## 六、整理层：日报 + 口播稿

### 让模型碰不到确定性内容

上一版让模型直接写完整 markdown，结果它把 GitHub 用户名 `RachelXiaolan` 复述成
`RachelXiaelan`，还把改名前的仓库名写了回去。

**URL、issue 号、任务短名这些都是确定性已知的，没理由让模型猜。**现在模型只输出内容块：

```
[1] done | 一句话总结
- done | 子弹点
- wip | 子弹点
TODO
- [1] 跟任务 1 有关的待办
```

代码按 `task.issue_key` 拼链接、按标题派生短名、按 `done/wip/blocked` 渲染 ✅⭕️❌。
**模型碰不到就写不错。**

任务短名支持别名（`fecho bind AI-2224 --alias 闲鱼选品`），
避开「#20260707-leo-1 与小Lu商量…」这种以工单编号开头的标题被截断。

### 其他几处

- **预算按任务数给**：固定 4000 token 时 28 条进展的日报被截断在半句话上，后两个任务只剩标题。
  现在 `max(4000, 1200 × 任务数 + 2000)`，解析出的任务数不足还会翻倍重试。
- **状态判断给依据**：把扫描时判好的 `kind`（坑/决定）透给模型。之前不给，它把所有条目
  都标成了 done，包括「只读 UI 先不做」这种明显没完成的。
- **兜底稿**：LLM 挂了照样出同样结构的稿，内容用进展原文顶上。日报和口播稿各自独立降级。

---

## 七、接口层

### MCP（9 个工具）

```
log_progress    记一条进展            catch_up      开工时拉回昨天的日报和未完成任务
my_tasks        在推进的任务          get_my_log    某天的流水
end_of_day      出日报 + 口播稿       fecho_doctor  自检
mobius_login    浏览器授权            sync_issues   同步在办 issue
team_digest     团队汇总
```

三种传输，**共用同一个 `handle()`**——工具集不按客户端分，谁连上都是同一套：

- **stdio**：`fecho-mcp`，Claude Code / Codex 用
- **`/mcp`**：Streamable HTTP
- **`/sse/`**：SSE，云端 ChatGPT 要的（URL 必须以 `/sse/` 结尾）

### CLI（14 个子命令）

```
doctor  login  setup  sync  scope  bind  scan
dedupe  web    hidden digest team   install serve
```

### Dashboard（`fecho web`）

本机 HTTP 服务，一个进程同时托 MCP 端点和 dashboard。四块面板：

1. **归属复核 + 一键改** ← 核心。日报准不准取决于归属对不对，而这件事此前
   **没有任何人工纠正入口**，只能敲命令行。
2. **被挡掉的进展** —— 一键捞回
3. **7 天任务时间线** —— 写周报的原料
4. **归属健康度** —— 落自由任务的比例

鉴权：本机随便连；非本机必须带 `FECHO_WEB_TOKEN` 的 bearer；**没设 token 时直接拒绝外部来源**
——隧道一开 `/sse/` 就在公网上了，宁可连不上也不要默认裸奔。

---

## 八、团队联邦

collector 只存**成品**（日报 + 口播稿），**物理上没有 `entries` / `tasks` 表**。

这不是靠权限管住的隐私边界——**是收集端的库里压根不存在能读到原始进展的表**。

---

## 九、Mobius 集成

OAuth 2.1 完整实现：动态客户端注册 + PKCE + 公开客户端，浏览器一次授权，零配置。

**项目绑定**是另一个关键机制：`fecho bind AI-2541` 把当前目录绑到一个 issue。
解决的是「做这个项目本身」时，说的话和 issue 标题字面上一个词都不重合的问题——
工作目录是个几乎免费的强信号，绑一次就够。

---

## 十、测试

78 个单测，16 个测试组：

```
TestMatching             配对的四条路径
TestNoSemanticGuessing   字面相似度不该用来判归属（已删的行为要锁住）
TestScanDoesNotCascade   一条错不能带偏后面所有条
TestScanAssignsIssues    LLM 判归属 + 候选列表校验
TestDedupeKeepsData      判为重复的照样存库
TestCrossValidation      出稿前重判，含明确填的
TestWebEndpoints         HTTP / SSE / 鉴权
TestDailyFormat          链接、短名、状态图标由代码渲染
TestScanWatermark        失败时水位线停在那组之前
TestCollector            联邦只收成品
… 另有 Aggregation / LLMRetry / Digest / Scoping / Scope / ProjectBinding
```

测试里有几条是**真实翻车的复现**，注释直接写明了案发经过——
比如 `TestNoSemanticGuessing` 锁的就是「闲鱼选品被 `agent` 一个词配进日志系统」。

SSE 那个测试起真的 uvicorn 跑完整往返：中途写过一版「只验第一个事件」的，
但那版断言的是硬编码字符串、根本没碰到真代码，是自己骗自己，删了。
**SSE 是两条腿的协议，只验一条腿等于没验。**

---

## 十一、当前状态与遗留

### 能用的

- MCP（stdio / HTTP / SSE 三种传输）
- 自动扫描 + 水位线 + 工作范围白名单
- 语义归属 + 交叉验证
- 日报 + 口播稿（含兜底降级）
- Dashboard
- 团队联邦收集端
- Mobius OAuth + 项目绑定

### 遗留问题

1. **只在一个人、一周数据上验证过。** 这是最大的未知。机制看着通用，但没有第二个人的数据
   能证明。dashboard 上的「归属健康度」就是为这个准备的预警。

2. **`mobius-auto` 还有 5 条历史残留**，是删除关键词配对之前留下的数据。

3. **`_COMMON_TOKENS` 停用词表还在**，现在服务于去重（文本对文本，这个用法站得住），
   但它当初是为配对加的，`SCAN_DEDUPE_SIMILARITY = 0.62` 同样是照一天数据标出来的。

4. **历史任务未合并。**「把 fecho 装到本机」这类任务在数据里还是独立的，
   它本该是 AI-2541 的活。verify 只重判进展归属，不合并任务。

5. **Dashboard 页面太长**（9798px），时间线把 7 天进展全铺开占掉大半。

6. **`#comment-xxx` 锚点链接做不了**，fecho 拿不到评论 id，只能链到 issue。

### ChatGPT 接入

**桌面版 ChatGPT 就是 Codex**（bundle id `com.openai.codex`），配置在 `~/.codex/config.toml`，
支持 stdio 和 HTTP 两种 MCP。**在本机的话不需要隧道**——直接用 stdio 入口 `fecho-mcp` 即可，
跟 Claude Code 走同一条路。

`CONNECT-CHATGPT.md` 里那套 cloudflared 隧道方案是按「ChatGPT 在云上」写的，
**前提不成立时应当删除或重写**。

---

## 十二、这个项目里反复出现的几条判断

**1. 确定性的东西不要交给模型。** URL、issue 号、任务短名、状态图标——这些代码都知道，
让模型复述只会引入错误。模型碰不到就写不错。

**2. 调参是在背答案。** 停用词表、相似度门槛，都是照着一天的数据凑出来的。
换个人、换个行业，词汇表完全不同。能换成语义判断的就别调参。

**3. 判错了要留后路。** 归属有三层重判，去重有 `hidden` 能捞回。
**没有纠错机会的破坏性操作，代价要算清楚**——留一行的成本几乎为零，丢一条真实进展的成本很高。

**4. 改了要说出来。** 交叉验证改了归属会写进 warnings，不闷声改。

**5. 只验一条腿等于没验。** 测试要碰到真代码，断言硬编码字符串是自欺欺人。
