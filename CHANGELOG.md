# 版本记录

版本号写在 `pyproject.toml` 和 `fecho/__init__.py`，两处要一致。每个版本在 git 上打 `vX.Y.Z` 标签。
按框架环节标注：①采集 ②归属 ③存储 ④整理 ⑤交付 ⑥登录·运行时。

## 0.8.4 · 2026-09-17

### ② 归属：任务别再拆碎

真实情况：Rachel 名下 241 个任务，237 个是自由任务，其中 142 个一条进展都不挂。两个原因叠在一起：

- **扫描把一场对话拆成十几个任务**：扫描来源故意关掉了「跟着同一对话里上一个任务」（怕一条判错带偏一片），
  结果没写 issue 号的条目每条都新建。一场 Codex 对话最多被拆成 21 个
- **重判来回翻，积出空壳**：出日报前会把当天归属重判一遍，某条算不算 AI-2541 会来回翻；
  每次翻回自由任务都新建一个，原来那个不删，每翻一次多一个空壳

改动：
- 扫描条目没写 issue 号时，归进「同一天 + 同一场对话」已有的自由任务（匹配方式记为 `session-group`）。
  **只并自由任务，从不往 issue 上塞**，所以当初「一条判错带偏一片」的顾虑不成立；写了 issue 号的照样单独归
- 重判把进展挪回自由任务时先复用：同一场对话的那个 > 这条之前待过的那个 > 才新建
- 自由任务上的进展全挪走了就收起来（`status=empty`），不在任务列表里占位；有进展挪回来会重新打开
- 新命令 `fecho tidy-tasks`：收拾存量——空壳收起，同一天同一场对话的碎片合成一个。默认只演练，
  加 `--apply` 才动手；合并**不上锁**，之后的重判还能把某条挪去对的 issue
- 网页上任务名优先显示模型起的短名：自由任务的标题是第一条进展的原文，当名字认不出是哪件事

## 0.8.3 · 2026-09-16

### ④ 整理
- **修：任务多的日子日报掉成兜底稿。** 解析出的任务块少于任务数时会重试，重试额度原来写的是
  `min(首次预算*2, 上限)`——首次预算超过上限时，`min` 反而把额度压小了（48 个任务：
  59600 → 16000），重试比第一次还弱，推理把额度烧光、正文为空，整篇降级成兜底稿。
  改成只能加不能减。9/16 Rachel 那份日报就是这么坏的（102 条进展 / 48 个任务，当天只有她中招）
- 一次调用的 token 上限从写死的 16000 改成可配：`llm_max_tokens` / `FECHO_LLM_MAX_TOKENS`，
  默认 32000。首次预算也按这个上限封顶，不再算出端点根本不接受的数

### 记一笔
- 技术债新增：任务多的日子日报要**分批生成**（每批 10~12 个任务）。这一版只修了重试变小的 bug，
  任务足够多时一次调用仍然写不完。见 `docs/tech-debt.md`

## 0.8.2 · 2026-09-16

### ① 采集
- 认出 Grok：名字里带 `grok` 的客户端都算（`grok-bot`、`grok-cli`、`Grok`）。
  之前 leo 的 grok-bot 连上来认不出，网页上看不到它接入，随手记的来源只是一串生名字
- 设置页多一张 Grok 卡片（徽章 GK），显示已接入 / 未接入；扫描开关和 Cursor 一样置灰：
  Grok bot 是服务器上的 application，本机既叫不醒它、也没有它的聊天记录
- 安装说明加「你是 Grok bot（或别的能连 MCP 的 agent）」：通用的 Streamable HTTP MCP 接法，
  并写明**token 是谁的，记录就进谁的账号**——共用一个 bot 时每人要填自己的 token
- 修 MCP 的 `submit_scan` 工具描述：原来写「claude-code / codex / hermes」，实际只有前两个能扫

### ⑤ 交付
- 改日报时可以开预览：编辑栏加「显示预览 / 关掉预览」，默认关着，开关状态记在浏览器里。
  开着时左边写、右边实时渲染，窄屏改成上下排。0.8.1 把预览整个删掉了，这版改成可开关的

### 记一笔
- 技术债新增：日报编辑器的 CodeMirror 6 小样（要「光标移过去露出 `##` / `- [ ]`」那套 Obsidian 语义，
  Milkdown 那类块编辑器做不到），优先级靠后。见 `docs/tech-debt.md`

## 0.8.1 · 2026-09-16

### ⑤ 交付 · 改日报的编辑框（照着 Obsidian 的习惯）
- 在 `- ` / `1. ` / `- [ ] ` 那行末尾按回车，下一行自动接着写：编号加 1，做完的待办后面接一个没做的 `[ ]`
- 在只有列表符号的空行上再按回车：有缩进往外提一层，没缩进就退出列表
- Tab / Shift+Tab 缩进、提出选中的行；挪到新一层的编号从 1 开始
- 回车、Tab、删行、粘贴之后自动重排编号：只排光标所在的那一个列表，
  空行隔开的算两个列表，各自从头数（Obsidian 1.8.4 那版就是连着数才出的问题）
- 改动走浏览器自己的输入，Cmd+Z 能撤销；中文输入法选字时的回车不当换行

### ⑤ 交付 · 其他
- 去掉编辑时右边的实时预览，回到只有一个编辑框
- 图片宽度改成：光标停在图片那一行时，编辑框上方出现宽度滑块，拖动直接改原文里的 `|宽度`
- 看日报时折起来的项后面显示 `…`；折叠状态按人、日期、标签页记在浏览器里，
  刷新后还折着，日报内容变了就不再套用

## 0.8.0 · 2026-09-16

### ① 采集
- 本机采集支持 Windows 10/11 和 Linux（之前只支持 Mac）：
  - 定时任务按系统装：macOS 用 launchd，Linux 有 systemd 用 systemd 用户定时器、没有就用 cron，
    Windows 用任务计划程序 `FechoCloudCheck`（用 pythonw，不弹窗口）
  - 路径统一成正斜杠比较；Windows 盘符算绝对路径，盘符部分不分大小写。本机脚本和服务器用同一套规则
  - 叫 agent 提炼时统一按 UTF-8 读写，Windows 上不弹黑框；桌面通知按系统用 osascript / PowerShell / notify-send
- 安装说明按系统给命令（Windows 在 PowerShell 里跑）；网页上手动加文件夹接受 `C:\...` 这种路径
- 接入指南写明支持 macOS、Windows、Linux，并标注 **Windows 和 Linux 还没在真机上完整验证过**

### 升级注意
- Mac 上的行为不变。Windows / Linux 同事装完或第二天没看到扫描结果，请在「反馈」里附上 `status` 的输出

## 0.7.0 · 2026-09-16

### ① 采集
- 设置页 Claude Code 卡片加一行说明：含桌面版和命令行的 Code 对话，扫描用命令行 `claude` 的登录和配置
- 每晚扫描交上来的记录带上入口（`claude-desktop` / `cli`），「今天」页显示成「claude-code · 桌面版」。
  需要同事重装一次本机采集才生效
- Cursor 加进 agent 列表：能显示接入状态、随手记能认出来源；对话本机暂时扫不了，扫描开关置灰
  （缺什么见 `docs/tech-debt.md`）。安装说明加上接 Cursor 的写法

### ④ 整理
- 还没生成日报也能先手写。人写的或改过的日报，到点生成、补扫重出都不覆盖它，
  自动生成的版本只放进「历史版本」；事实没变就不重复出。只有人点「重新生成」才换成自动版
- 历史版本记下每一版是按什么事实写的（`report_history.fingerprint`）

> **这一版动了表结构**：新增 `report_images` 表、`report_history` 加 `fingerprint` 列。
> 线上入口 `api/index.py` 不建表，部署完必须照 DEPLOY.md「改了表结构之后」跑一次建表，否则
> 保存日报和日报贴图都会 500。0.8.1 上线时就漏了这一步。

### ⑤ 交付
- 日报按 Obsidian 写法显示：`- [ ]` / `- [x]` / `- [/]` 等待办状态，`> [!note]-` 可折叠提示框，
  有子项的列表和标题可折叠，表格，代码块
- 日报里能贴图：粘贴、拖入或点「插入图片」，在预览里拖右下角调大小（写回 `![图|宽度](地址)`），
  点图看大图。图只有本人和 admin 能打开（新表 `report_images`）
- 改日报时左边写、右边实时预览
- 切日期：看过的旧日期直接显示上次读到的，不再每次重新加载；只有今天自动刷新；工具栏加「刷新」
- 加载提示不再说「正在整理工作现场」（本来就只是读记录）；看旧日期时标题写「这天推进了 N 件事」
- 所有页面（面板、登录、接入、接入指南）加白天 / 黑夜模式切换，默认跟系统，选择记在浏览器里

## 0.6.1 及以前

云端版上线（Vercel + Supabase + lu2 后台程序）、Mobius 登录、本机采集、设置页与管理员、
中英文界面、手动改日报和写作偏好、接入指南、反馈收集区。明细见 git 历史。
